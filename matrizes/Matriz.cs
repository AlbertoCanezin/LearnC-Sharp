/*
using System;

public class Program
{
    static void Main(string[] args) {
        // A virgula indica que estamos criando um arranjo bidimencional (matriz)
        double[,] matriz = new double[2, 3];

        // Verificando o tamanho total da matriz 
        Console.WriteLine(matriz.Length);

        // Verifica a primeira dimensão da matriz (quantidade de linhas)
        Console.WriteLine(matriz.Rank);

        // Vericando o tamanho da dimensão da matriz (quantidade de linhas)
        Console.WriteLine(matriz.GetLength(0));

        // Verificando o tamanho da dimensão da matriz (quantidade de colunas)
        Console.WriteLine(matriz.GetLength(1));
    }
}
*/